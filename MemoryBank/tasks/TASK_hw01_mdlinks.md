# TASK hw01_mdlinks — CLI: Markdown Link & Dead Code Checker

- **Статус**: ✅ **DONE 2026-08-17** — модуль написан, отлажен, покрыт тестами, задокументирован;
  ✅ **этап 2 закрыт 2026-08-17** — боевая приёмка на реальных данных, ускорение −36 %, устойчивость, ревью
- **Пакет ДЗ**: `homework/hw01_mdlinks/` (`task.py`, `solution.py`, `support/`, `README.md`)
- **Модуль**: `core/mdscan/` (сканер) + `core/tokenstat/` (учёт токенов)
- **Документация модуля**: `Doc/Modules/mdscan/README.md` (архитектура) ·
  `Doc/Modules/mdscan/CLI.md` (командная строка, все параметры, отчёт)
- **Создан**: 2026-08-16 · **Закрыт**: 2026-08-17

## Условие (кратко)

Консольная утилита: сканирует папку → находит `.md` → извлекает ссылки `[text](url)` и
`file:///...` → проверяет локальные (существование файла) и внешние (HTTP HEAD/GET) →
печатает отчёт с цветами/таблицей и статус-кодами.

Подводные камни, названные в задании:
- относительные пути разрешаются **относительно файла**, где встретилась ссылка;
- таймауты HTTP и обработка сетевых ошибок (404 / 500 / timeout).

Полный текст — `homework/hw01_mdlinks/README.md`.

## Что сделано

Разработка шла по ТЗ `MemoryBank/tasks/TASK_hw01_modules_T01-T15.md` (таски **T-01…T-16**),
шестью волнами; таски внутри волны выполняли независимые агенты Opus, приёмку делал
оркестрант (скилл `.claude/skills/hw01_dev_test-build/`).

| Волна | Таски | Что закрыто |
|---|---|---|
| 0 | T-01 · T-02 · T-03 · T-04 · T-15 | модели и enum, фикстуры-дерево, конфигурация, логирование, tokenstat |
| 1 | T-05 · T-06 · T-07 · T-08 · T-09 | CLI и валидация, парсинг, чекеры, источники репозиториев, обход |
| 2 | T-10 · T-11 · T-12 | конвейер, прогресс, отчёты |
| 3 | T-13 | `Scanner` / `ScanOrchestrator` / `PipelineRunner`, точка входа `python -m core.mdscan` |
| 4 | T-14 | `Hw01MdLinks(HomeworkTask)`, метрики ДЗ, `python run_hw.py hw01` |
| 5 | T-16 | документация: README ДЗ, `Doc/Modules/mdscan/`, MemoryBank |

Один агент (T-13) упал на лимите сессии и был перезапущен — работа принята со второго прохода.

### Итог по коду и тестам (после этапа 2)

- `pytest tests/hw01 -q` → **444 passed, 4 skipped** (17 тестовых файлов; skip — живые HTTP-проверки
  `test_http_live.py`, идут только с `MDSCAN_NETWORK=1`).
- `mypy core homework` — 0 ошибок (141 файл), `ruff check core homework tests/hw01` — чисто.
- Сеть в обычных тестах только `127.0.0.1` (локальный `http.server`); `sleep` и `TestRunner`
  не использованы.

### Метрики прогона `python run_hw.py hw01` (набор A, `out/hw01/metrics.json`)

| метрика | значение |
|---|---:|
| `md_files_total` / `files_ok` / `files_failed` | 28 / 27 / 1 |
| `links_total` | 82 |
| по категориям | local 54 · anchor 7 · url 9 · github 6 · mailto 3 · tel 1 · wikilink 1 · footnote 1 · unknown 0 |
| `broken_total` (local / anchor) | 7 (5 / 2) |
| `error_rate` | 0.036 |
| `extract_f1` (порог 0.95) | **1.000** |
| `classify_accuracy` (порог 0.98) | **1.000** |
| `duration_sec` / `throughput_files_per_sec` | 0.058 / 485.6 |
| `speedup` / `parallel_efficiency` / `workers_used` | 0.947 / 0.095 / **10** |

`speedup < 1` — честный результат: без HTTP разбор упирается в GIL, потоки не помогают.
Ожидание спеки 1.5–3× относится к прогону с сетевыми проверками — там оно перекрыто:
**×9.59** на 10 потоках (локальный сервер 30 мс, 600 уникальных URL).

### Токены разработки — этап 1 (`out/hw01/tokens_2026-08-17_04-09-01.md`, полное окно)

| группа | requests | out | cache_create | cache_read | thinking |
|---|---:|---:|---:|---:|---:|
| агенты (17 запусков: T-13 перезапускался) | 513 | 672 418 | 2 337 444 | 62 018 083 | 266 782 |
| оркестрант | 114 | 90 188 | 412 688 | 29 671 539 | 5 882 |
| **всего** | **627** | **762 606** | **2 750 132** | **91 689 622** | **272 664** |

Итого по всем полям — **95 203 656** токенов. (Промежуточный срез
`out/hw01/tokens_2026-08-17_03-51-18.md` — 572 запроса / 84.9 млн — снят до закрытия волны 5.)

## Этап 2 — боевая приёмка, ускорение, устойчивость, ревью ✅

Спека этапа — `MemoryBank/specs/hw01_mdscan_hardening_spec_2026-08-17.md`,
таски **H-01…H-13** — `MemoryBank/tasks/TASK_hw01_hardening_H01-H12.md`, скилл `/hw01-build`.
Пять волн одним заходом, 13 запусков агентов Opus, приёмка оркестрантом.

| Таск | Что закрыл | Числа | Отчёт |
|---|---|---|---|
| **H-01** | прогон по живой организации GitHub `dsp-gpu` (10 приватных репо, SSH-клоны, HTTP включён) | 1230 `.md` / 2669 ссылок / 634 битых / 103 с / код 1, 0 `ERROR`, клоны удалены, секретов в логе нет; счётчики совпали с эталонным прогоном до единицы | `specs/hw01_h01_org_run_2026-08-17.md` |
| **H-02** | большой локальный репозиторий, три прогона (без сети / с сетью / с вложенными) | 846 / 1063 → 7.3 с · 19.7 с; с вложенными 1629 / 3263 → 29.0 с; `.gitignore` сошёлся файл-в-файл (1629 = 846 + 783) | `specs/hw01_h02_local_run_2026-08-17.md` |
| **H-03** | живые HTTP-исходы `OK` / `BROKEN` / `TIMEOUT` + 4 теста `@pytest.mark.network` | 15 ссылок → 9 / 3 / 3; при `timeout_ms:100` медленные уходят в `TIMEOUT`, DNS-ошибка остаётся `BROKEN`; кэш подтверждён замером 0–1 мс против 166–182 мс | `specs/hw01_h03_http_live_2026-08-17.md` |
| **H-04** | база замеров + инструмент `bench_scan.py` | набор B, 1 поток — 0.605 с; слои; 4395 вызовов `_getfinalpathname`, 50 638 `logging.debug`; `cumtime` профайлера при потоках — артефакт | `specs/hw01_h04_baseline_2026-08-17.md` |
| **H-05** | блочный разбор заголовков + обход без двойного `resolve()` (`ResolvedPathCache`, `os.scandir`) | обход 0.121 → 0.060 с; проверка 0.186 → 0.082 с; резолвов 4395 → 536 | вошёл в отчёт H-07 |
| **H-06** | ленивое логирование, одна `WARNING` на битую ссылку, `http cache` в логе | накладные 0.166 → 0.109 с | вошёл в отчёт H-07 |
| **H-07** | повторный замер и вывод по параллельности | **0.605 → 0.389 с (−36 %)**, нагрузка 0.319 → 0.225 с; `speedup` без HTTP 0.90 (10 потоков) / 0.96 (5); с HTTP **×9.59** / ×5.15; живая сеть ×1.74 на 8 файлах | `specs/hw01_h07_after_2026-08-17.md` |
| **H-08** | 7 аварийных сценариев + `tests/hw01/test_resilience.py` (12 тестов) | `Ctrl+C`: было — трейсбек и процесс живёт > 2 мин; стало — гашение 0.32 с, код **130**, `CRITICAL`, потоков нет. `429` → код 3 с временем сброса; битый репозиторий не роняет прогон; файл 10 МБ — 2.25 с | `specs/hw01_h08_resilience_2026-08-17.md` |
| **H-09** | нагрузка 500 / 2000 / 5000 файлов | 0.390 / 1.360 / 3.255 с — линейно (r² = 0.99997); память не течёт (пик 28.0 МиБ ×3 прогона, остаток 0.1 МиБ); backlog до 98 % дерева, ≈4.6 КиБ/файл; `DEBUG` = ×2 времени | `specs/hw01_h09_load_2026-08-17.md` |
| **H-10** | ревью соответствия спеке | требований раздела 0 закрыто 23/24 (A11 «документация» закрывает H-12); инвариантов с тестом 23/25 полностью + 2 частично; контрактов `Protocol` сверено 15/15; расхождений **Р-01…Р-15**, критичных 3 | `specs/hw01_h10_review_spec_2026-08-17.md` |
| **H-11** | ревью кода (~120 файлов) | 1 🔴 · 10 🟠 · 34 🟡 · 8 «оставляем осознанно»; 7865 строк продуктивного кода против бюджета ≈1940; мёртвого кода и тестов-пустышек не найдено | `specs/hw01_h11_review_code_2026-08-17.md` |
| **H-13** | параллельное клонирование организации (`clone_workers = workers.discover`) | `dsp-gpu` без HTTP: **45.2 → 31.5 с** | замеры `out/hw01/h13/` |
| **H-12** | документация и MemoryBank | README ДЗ, `Doc/Modules/mdscan/README.md` + `CLI.md`, пометки `🔧 этап 2` в спеках, MemoryBank, сводный список находок | этот файл |

Дополнительно оркестрантом (ревью 6 и волна 1): `OtherSchemeRule` (10-е правило классификации),
обрезка ячеек отчёта до 200 символов, секция отчёта «HTTP 401/403/429 — вероятно защита от ботов»,
`linkify` только со схемой, умолчания `workers.parse` / `http.workers` 5 → **10**, `report.console`,
`RepoInfo.scope`, CLI `python -m core.tokenstat`.

**Токены этапа 2 (финал, с H-12)**: 737 запросов, **128 947 191** токенов (агенты 664 запроса / 587 704 output /
87.7M cache_read; оркестрант 73 / 52 294 / 37.8M) — файл `out/hw01/tokens_2026-08-17_10-09-28.md`
(срез до H-12 — `tokens_2026-08-17_09-44-33.md`). Разбивка по таскам H-01…H-13 —
в `homework/hw01_mdlinks/README.md`, раздел «Стоимость разработки в токенах».
**Оба этапа вместе**: 1364 запроса, ≈**224.2 млн** токенов, 31 запуск агентов.

## Документы

| Что | Где |
|---|---|
| ТЗ по таскам T-01…T-16 | `MemoryBank/tasks/TASK_hw01_modules_T01-T15.md` |
| Часть 1/2 — решения D1–D19 | `MemoryBank/specs/hw01_mdscan_reasoning_2026-08-16.md` |
| Часть 2/2 — архитектура | `MemoryBank/specs/hw01_mdscan_architecture_2026-08-16.md` |
| Правила разработки и тестирования | `MemoryBank/specs/hw01_mdscan_dev_test_spec_2026-08-16.md` |
| Ревью 2–5 (списки правок) | `MemoryBank/specs/hw01_mdscan_review{2,3,4,5}_fixes_2026-08-16.md` |
| Ревью 6 (перед этапом 2) | `MemoryBank/specs/hw01_mdscan_review6_fixes_2026-08-17.md` |
| Спека этапа 2 (боевая приёмка, скорость, устойчивость) | `MemoryBank/specs/hw01_mdscan_hardening_spec_2026-08-17.md` |
| ТЗ по таскам H-01…H-13 | `MemoryBank/tasks/TASK_hw01_hardening_H01-H12.md` |
| Отчёты этапа 2 | `MemoryBank/specs/hw01_h0{1,2,3,4,7,8,9}_*.md`, `hw01_h1{0,1}_*.md` |
| Архитектура модуля | `Doc/Modules/mdscan/README.md` |
| CLI, конфигурация, отчёт | `Doc/Modules/mdscan/CLI.md` |
| Условие, метрики, выводы | `homework/hw01_mdlinks/README.md` |

⛔ `MemoryBank/specs/hw01_mdscan_spec_2026-08-16.md` — ранний вариант ТЗ, не использовать.

## Решения по ходу

- **2026-08-16**: HTTP-проверка в v1 (отключаемо ключом), «Dead Code» = мёртвые ссылки,
  код в `core/mdscan/`, консоль Strategy `rich` + ANSI-fallback, CLI только позиционный,
  golden-набор (эталонное дерево) делаем.
- **2026-08-16 (ревью 3–5)**: `MarkdownWorker(BaseObserver)`, отчёт строит главный поток после
  `join()`, `check(link, md_file)`, `HttpChecker` — один на прогон, вид цели определяется один раз
  правилом V5.
- **2026-08-17**: `file:///…` → `SKIPPED` (иначе ложная битая ссылка); `RepoInfo.scope` для цели-
  подкаталога; `linkify-it-py` в extra `hw01` (без него `gfm-like` гасит `linkify`); `wikilinks` —
  встроенное правило экстрактора, такого плагина в `mdit-py-plugins` нет; на Python 3.14
  `threading.Thread._context` конфликтует с одноимённым полем наследника.

## Решения по ходу — этап 2

- **2026-08-17 (волна 1, оркестрант)**: `data:`-URI больше не «локальный путь» — правило
  `OtherSchemeRule` перед тотальным `LocalPathRule`; ячейки отчёта обрезаются до 200 символов;
  `401/403/429` — отдельная секция отчёта; `linkify` только со схемой.
- **2026-08-17 (H-05/H-06)**: заголовки для якорей берутся **блочным** разбором (`markdown-it`
  без inline-правил), обход — `os.scandir` + один `resolve()` на файл, чекеры логируют `DEBUG`,
  единственная `WARNING` на битую ссылку — у воркера (у него есть контекст repo/file).
- **2026-08-17 (H-08)**: `KeyboardInterrupt` — не `Exception`, поэтому его ловят отдельно;
  прерывание отбрасывает очередь, **возвращая сентинел на место**, и даёт код **130** без отчёта
  (неполные числа в отчёте не отличить от полных).
- **2026-08-17 (H-13)**: клоны организации — своим пулом на `workers.discover` потоков, отдача
  `RepoInfo` по мере готовности; контракт `RepositorySource` не менялся.

## Сводный список находок H-01…H-11

### ✅ Исправлено на этапе 2

| # | Что | Кто / где |
|---|---|---|
| Д-1 | `data:image/…;base64` → `LOCAL` → ложный `BROKEN` + строка отчёта 1.8 МБ | оркестрант: `parsing/rules/rule_other_scheme.py` + `link_classifier.py`; `reporting/markdown_report_builder.py` (`_MAX_CELL = 200`), 6 тестов |
| Д-2 | клоны организации шли последовательно (72.5 с из 103 с) | **H-13**: `source/github_org_source.py` (пул `clone_workers`), `source_factory.py`; 45.2 → 31.5 с |
| Д-3 | битая ссылка = два `WARNING` (чекер + воркер), 1269 строк на 634 ссылки | **H-06**: чекеры → `DEBUG`, единственная `WARNING` в `runtime/markdown_worker.py` |
| Д-6 | попадание в HTTP-кэш неотличимо в логе | **H-06**: префикс `http cache` в `checking/http_checker.py` |
| Д-H08-1 | `Ctrl+C`: трейсбек, лог закрыт раньше воркеров, процесс жив > 2 мин, нет кода возврата | **H-08**: `runtime/pipeline_runner.py` (`cancel`/`_await_tasks`), `scan_orchestrator.py` (код 130), `__main__.py`; 5 новых тестов |
| Р-15 / Д-7 | `std::string`, `af::array` из автодоков → битые локальные | закрылось побочно правилом `OtherSchemeRule` (схема ≥2 букв + `:` → `UNKNOWN`); проверено H-10 |
| — | fuzzy-`linkify` превращал `Full.md`, `conftest.py`, `prng.cl` в URL — 51 ложный `BROKEN` | оркестрант (ревью 6): `fuzzy_link=False` + тест |

### ⚖️ Оставлено осознанно (с обоснованием)

| # | Что | Почему оставляем |
|---|---|---|
| Д-4 | `broken_total` включает `TIMEOUT` | по определению `MdFileResult.broken_count` = `BROKEN` + `TIMEOUT` (T-01). В таблице по категориям колонки разные; **задокументировано** в `CLI.md` §7.1 |
| Д-10 | `duration_sec` не покрывает вывод сводки в консоль (7 с `rich` на 634 строки) и удаление клонов | ожидаемо по D6: фазы 2–3 вне замера. **Задокументировано** в `CLI.md` §7.1 |
| H-09 З-1 | очереди без `maxsize`: backlog до 98 % дерева, ≈4.6 КиБ на файл → ≈0.5 ГБ на 100 тыс. файлов | ограничение архитектуры (D1/D3: завершение по сентинелам, `qsize` потоком не управляет). **Задокументировано** как граница применимости |
| H-07 §8.3 | цель «блочный разбор ≤ 30 % от полного» не выполнена (49 %) | оценка в спеке была оптимистичной: выключены только inline-правила, блочная токенизация остаётся. Цель **по времени** слоя «проверка» закрыта (0.186 → 0.082 с) |
| H-07 §8.4 | планка `speedup ≥ 0.95` без HTTP не достигнута (0.90 при 10 потоках) | планка ставилась к старому знаменателю: абсолютное время улучшилось на всех точках (0.657 → 0.432 с), а цена потоков почти постоянна (+0.04 с). По 5 потокам — 0.96, в планку |
| — | `ProcessPoolExecutor` для обхода GIL | ломает правило владения объектом (D15); старт процессов на Windows съест выигрыш на задаче в 0.4 с |
| H-11 §4 | мёртвый код и тесты-пустышки | искали AST-скриптами — **не найдено**; голых `except`, `print()` вне слоя вывода, `if/elif` по типу тоже нет |

### ❓ Открытые вопросы к Alex после этапа 2

Ничего из этого списка **не исправлялось** — все пункты меняют либо наблюдаемое поведение,
либо согласованные документы.

**A. Поведение сканера**

| # | Вопрос | Варианты |
|---|---|---|
| 🔴-1 | Консоль считает «битых» **сама** (`console_renderer.summary_rows` обходит `results`), а не берёт числа у `StatisticsCollector`: на экране «битых 1», в счётчиках `broken_total 2`. Прямое нарушение правила 07 и докстринга самого `StatisticsCollector` | брать все числа из `summary.counters` (≈10 строк). **Рекомендую первым** — закрывает и путаницу Д-4 |
| Д-4 | Семантика `broken_total` | (а) оставить `BROKEN + TIMEOUT` и переименовать в `broken_or_timeout`; (б) не включать `TIMEOUT`. От этого зависят числа в README ДЗ |
| Д-8 | `-scan.include_nested_repos:true` подхватывает `.claude/worktrees/*` — git-worktree того же репозитория, явно перечисленный в `.gitignore` родителя (399 файлов, 24 % прогона) | (а) фильтровать найденные корни через `git check-ignore` родителя одной пачкой; (б) отличать worktree по содержимому файла `.git`; (в) оставить и документировать — сейчас так |
| Д-9 | `http.timeout_ms` не покрывает DNS: `getaddrinfo` на несуществующий домен — 10–12 с при любом значении; и не является общим бюджетом запроса (редиректы получают таймаут заново) | (а) `socket.setdefaulttimeout` в Composition Root; (б) резолв имени в отдельном потоке со своим таймаутом; (в) оставить и документировать — сейчас так, записано в `CLI.md` |
| Д-H08-3 | Код возврата **3** уходит молча: на stdout/stderr ничего, причина только в файле лога. Прогон организации по исчерпанному лимиту выглядит как «ничего не произошло» | печатать причину кода 3 в stderr одной строкой — как точка входа уже делает для внутренней ошибки |
| 🟠-10 | `mdscan.yaml` (11 КБ) пишется в **текущий каталог** независимо от `-report.dir` — каждый прогон мусорит в рабочем дереве | (а) писать дамп в `report.dir`; (б) только по явному флагу; (в) оставить и документировать — сейчас так |
| H-09 З-2 | Размер отчёта линеен (≈158 байт на файл): на 100 тыс. файлов ≈15 МиБ Markdown, редакторы такое открывают плохо | предел строк в таблице битых + «показано 500 из N» — меняет полноту отчёта |
| H-07 §8.1 | 90 % вызовов `logging.debug` — чужие: `markdown_it` зовёт свой логгер на каждую попытку блочного правила, 44 746 из 49 585 | глушить чужой логгер в Composition Root — дёшево, но это правка поведения сторонней библиотеки |
| H-07 §8.2 | `speedup` в метриках ДЗ измерен «один прогон против одного», последовательный идёт первым на непрогретом кэше → гуляет ±6 % | прогрев + медиана в `Hw01Metrics` — меняет поведение ДЗ |
| 🟡-31 | `exit_code` в `metrics.json` **всегда 0** (`run.fail_on_broken = false`) — поле не несёт информации | считать «как было бы» (`broken_total > 0`) либо убрать из метрик |

**B. Документы и согласованные величины**

| # | Вопрос | Варианты |
|---|---|---|
| Р-08 | `progress.style` различается **только** значением `off`: `line` и `panel` дают одно и то же (вид выбирается по наличию `rich`) | (а) убрать `panel` из конфига и документации; (б) реализовать `panel` отдельным `ProgressView`. Пометка `🔧 этап 2` внесена в спеку §2 и в `CLI.md` |
| Р-09 | `source.auth` читается **только** при раскрытии организации: для одиночного репозитория протокол задаёт сам адрес, ключ молча ничего не меняет | поведение осмысленно — вопрос в формулировке. Пометка внесена в спеку §2 и `CLI.md`; менять ли поведение? |
| Р-11 | Бюджет кода §8: цель ≈1940 строк, факт **7865** (`core/mdscan` 6371 · `core/tokenstat` 613 · ДЗ 881). Причина — докстринги-обоснования по правилам 05/09 (≈половина строк) и 10 маленьких классов правил валидации | (а) считать бюджет по исполняемым строкам; (б) признать §8 справочным. Пометка внесена в спеку §8 |
| Р-12 / Д-11 | Имена потоков обхода — `discover_0`, `discover_1`; правило 11 требует `discover-1` | (а) префикс `discover-` (даст `discover-0` — нумерация с 1 у `ThreadPoolExecutor` не задаётся); (б) поправить формулировку правила |
| Р-10 | `report.console` реализован, но **ни одного теста** нет | добавить тест «`report.console: false` → в stdout ничего, файл отчёта на месте» — этого нет ни в одном таске |
| H-01 D-9 | Спека этапа §1.2 п.1 называет единственную не-`403` битую HTTP-ссылку «1 DNS»; фактически это ошибка проверки TLS-сертификата | правка текста спеки при следующем ревью |

**C. Рефакторинг (поведение не меняет, но правки утверждает Alex)**

| # | Что | Предложение H-11 |
|---|---|---|
| 🟠-1 | Коды «доступ закрыт» (`401/403/429`) заданы в **двух** местах | одна константа |
| 🟠-2 | Набор «ссылка не работает» определён **трижды** | один источник истины |
| 🟠-3 | `_scope()` — имя прогона считается **двумя** разными реализациями | один хелпер; иначе имена лога и отчёта однажды разъедутся |
| 🟠-4 + Д-5 | Секции «Битые HTTP» и «HTTP 401/403/429» — копия друг друга; когда все отказы `401/403`, первая печатает «нет» при `broken_http = 7` | склейка методов + перекрёстная подпись «все N — в секции …» |
| 🟠-5 | `ScanSummary` — `frozen=True`, но внутри изменяемый `dict` счётчиков | неизменяемое отображение либо честный VO |
| 🟠-8 | `ResolvedPathCache` создаётся **дважды** — кэш H-05 работает вполсилы | один экземпляр на прогон; проверяется `bench_scan.py --layers` (слой «обход») |
| 🟠-9 | `MarkdownFileFinder` сам создаёт `NestedRepoFinder`, и при `include_nested_repos: false` обходит всё дерево ради исключений | внедрять зависимость снаружи (DIP) и не обходить дерево зря |
| 🟠-6 · 🟠-7 | `source/github_org_source.py` — **318** строк, `reporting/markdown_report_builder.py` — **267**, `runtime/pipeline_runner.py` — **238** (бюджет правила 09 — 150) | дробление; делать **после** остальных правок, иначе строки поедут |

Приоритет, предложенный H-11: 🔴-1 → 🟠-1/2/3 → 🟠-8 → 🟠-4 → мелкие «бесплатные» проценты →
🟠-6/🟠-7 (дробление файлов) → и только после решений Alex — Д-8, 🟠-10, H-09 З-2.
