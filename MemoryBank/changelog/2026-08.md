# Changelog 2026-08

- **2026-08-17** — 🏁 **hw01 «Markdown / Git Scanner» собран и закрыт**: модуль `core/mdscan/`
  (сканер Markdown-ссылок: конфигурация `mdscan.yaml`, CLI с законом «первый аргумент — цель»,
  цепочка валидации V1…V10, три источника репозиториев, двухстадийный конвейер, чекеры
  local/anchor/http, Markdown-отчёт и консольная сводка, лог через `QueueListener`, прогресс)
  и `core/tokenstat/` (учёт токенов по JSONL-транскрипту). Разработка — **16 тасков T-01…T-16
  шестью волнами** независимыми агентами Opus с приёмкой оркестрантом (скилл `.claude/skills/hw01_dev_test-build/`);
  один агент (T-13) перезапускался после лимита сессии.
  Итог: `pytest tests/hw01 -q` → **396 passed** (15 файлов), mypy 0 ошибок, ruff чисто;
  `python run_hw.py hw01` → 28 файлов / 82 ссылки / 7 битых, `extract_f1 = 1.0`,
  `classify_accuracy = 1.0`, `speedup = 0.918` (честно: без HTTP разбор упирается в GIL).
  Токены разработки — 572 запроса, 84.9 млн токенов (агенты 615k output, оркестрант 82.5k,
  cache_read 81.7 млн), отчёт `out/hw01/tokens_2026-08-17_03-51-18.md`.
  Документация: `Doc/Modules/mdscan/README.md` (архитектура, C1–C4, D1/D4/D6, контракты,
  инварианты) и `Doc/Modules/mdscan/CLI.md` (закон CLI, все параметры конфигурации, коды
  возврата, разбор отчёта); README ДЗ заполнен реальными числами; TASK закрыт ✅.
- **2026-08-16** — развёрнут каркас репозитория: `.claude` (правила/хуки/скиллы/MCP), MemoryBank,
  `core/`+`common/`+`homework/`, CLI `run_hw.py`, тесты TestRunner, pyproject/README/шаблоны.
- **2026-08-16** — 🔄 смена стандарта тестирования: **pytest разрешён и стал стандартом**
  (решение Alex). Правило `04-testing-python.md` переписано, снята блокировка в хуке
  `pre_bash.sh`, `pytest`+`pytest-cov` в `[dev]`, добавлен `[tool.pytest.ini_options]`.
  Существующие наборы на `TestRunner` не тронуты — работают через `tests/all_test.py`.
  Добавлен образец `tests/test_metrics_pytest.py` + корневой `conftest.py`.
- **2026-08-16** — 📚 условие ДЗ №1 курса («CLI: Markdown Link & Dead Code Checker») перенесено
  из корневого файла в `homework/hw01_mdlinks/README.md` + `tasks/TASK_hw01_mdlinks.md`.
  Образец каркаса переименован `hw01_intro` → **`hw00_intro`** (`hw_id="hw00"`), чтобы номера
  `hwNN` совпадали с нумерацией курса; обновлены реестр, тесты, README, MASTER_INDEX.
- **2026-08-16** — 🔧 установлен **GitHub CLI `gh` 2.97.0** (portable, `%USERPROFILE%\.local\gh\bin`,
  добавлен в пользовательский PATH; winget/scoop/choco в системе нет). Нужен для получения списка
  репозиториев организации через API. Осталось `gh auth login` (интерактивно, делает Alex).
  Документация окружения → **`Doc/ENVIRONMENT.md`** (новый файл).
  Вход выполнен (`Logged in as AlexLan73`), `gh repo list dsp-gpu` вернул **10 приватных репо**:
  workspace, heterodyne, DSP, radar, signal_generators, core, spectrum, strategies, linalg, stats.
- **2026-08-16** — 📐 hw01: собраны две спеки — `specs/hw01_mdscan_reasoning_2026-08-16.md`
  (протокол рассуждений: decision log D1–D15, CLI 0–3 аргумента, C1–C4, метрики, риски) и
  `specs/hw01_mdscan_spec_2026-08-16.md` (ТЗ, таски T00…T11 с тест-гейтами).
  Добавлено правило `.claude/rules/09-oop-design.md` (ООП/SOLID/GRASP/GoF, один класс = один файл).
- **2026-08-17** — 🔍 **Ревью 6** этапа 2 hw01 (спека `hardening` + таски H-01…H-12 + скилл `hw01-build`):
  профиль пересчитан прямыми замерами (cProfile завышал `cumtime` в потоках), H-04/H-05 переписаны под реальное
  узкое место (двойной разбор `markdown-it`, обход), маркер `network` в `pyproject`, `python -m core.tokenstat` CLI,
  `TranscriptTokenMeter.start_from`; боевой прогон `dsp-gpu` (10 репо, 1230 файлов) — дефект fuzzy-linkify исправлен.
  Список — `specs/hw01_mdscan_review6_fixes_2026-08-17.md`.
