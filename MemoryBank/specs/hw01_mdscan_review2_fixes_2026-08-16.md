# Ревью 2 спеки hw01 (части 1/2 и 2/2) — что найдено и что исправлено

> **Дата**: 2026-08-16 · **Автор ревью**: Кодо (самопроверка по просьбе Alex: «глубокое ревью, сразу исправь»)
> **Статус**: ✅ прочитан Alex 2026-08-16, три вопроса закрыты (ответы внизу, внесены в спеку).
> Все правки в спеке помечены `🔧 ревью 2`.
>
> Файлы:
> - часть 1/2 — `hw01_mdscan_reasoning_2026-08-16.md` (решения D1–D19)
> - часть 2/2 — `hw01_mdscan_architecture_2026-08-16.md` (архитектура)

## Как проверяла

- Прочитала обе части целиком (1472 + 650 строк), сверила часть 1 ↔ часть 2 по каждому решению.
- Факты сверила с репозиторием и машиной, а не по памяти: `pyproject.toml` (extra `hw01` нет),
  `core/metrics` (какие функции есть), `~/.claude/projects/…/subagents/agent-*.jsonl` (где лежат
  транскрипты субагентов), `homework/hw01_mdlinks/README.md` (условие курса).

---

## 1. Ошибки проектирования (могли дать неверный отчёт или зависание) — исправлены

| # | Где | Что было не так | Что сделано |
|---|---|---|---|
| 1 | Ч1 D3.1 / D4, Ч2 C2, инварианты | `parse_workers` потоков читают одну `TaskQueue`, а `END_DISCOVERY` **один** — N−1 worker'ов зависли бы навсегда | Кладём **по сентинелу на каждый parse-worker**; на сентинел тоже `task_done()`. Инвариант 19 |
| 2 | Ч1 D3.1 / D4 | Не оговорён порядок `task_done()` ↔ `put(result)`. Если `task_done()` раньше — `TaskQueue.join()` в главном потоке проходит до того, как результат лёг в `ResultQueue`, и `END_RESULTS` **обгоняет результат** → тихая потеря | Правило: `task_done()` **только после** `put()`. Инвариант 18, проверяется макетом M1 |
| 3 | Ч1 D3.1, Ч2 C2/C4 | HTTP-пул (④) заполнял бы статусы в `MdLink` **после** того, как `MdFileResult` ушёл в `ResultQueue` — нарушение правила владения D15.2 (пишет один поток, потом только читают) | parse-worker отдаёт ссылки в http-пул, **дожидается**, заполняет статусы и только затем `put()`. Инвариант 20 |
| 4 | Ч1 D7.4, Ч2 §2, риски | `git ls-files` по умолчанию отдаёт **только tracked** файлы → новый незакоммиченный `.md` пропал бы молча | `ls-files --cached --others --exclude-standard`; вне git / `respect_gitignore: false` → `rglob`. Инвариант 17 дополнен |
| 5 | Ч2 §1.3 | Цепочка валидации: `OutputDirRule` (V7) шла **раньше** разбора `-поле:значение` (V9), хотя `-logging.dir:` задаётся именно там; нормализация пути (V10) — **последней**, после проверок существования | Порядок переставлен: V1 count → V2 first-is-target → V3 overrides → V4 normalize → V5…V7 target → V8 git-warning → V9 output dirs → V10 write |
| 6 | Ч1 D18 | Утверждение «токены субагентов пишутся в тот же поток сессии» — **неверно** (проверила): субагенты — отдельные файлы `<session>/subagents/agent-<id>.jsonl` | `TranscriptReader` читает главный jsonl + `subagents/*.jsonl`; агент → таск по ярлыку `TASK=…`, а не по времени |
| 7 | Ч1 D8, Ч2 C4/папки | Категории `WIKILINK`/`FOOTNOTE_URL` решены в D7.1 (✅ Alex), но в таблице D8.1, цепочке правил, `LinkKind` и папке `rules/` их **не было**; узнать wikilink по строке-цели нельзя (у `[[Page]]` нет схемы) | Добавлены `WikilinkRule`, `FootnoteRule` (9 правил); у `MdLink` поле `origin: LinkOrigin`, правило получает `MdLink`, а не `str`; enum `link_origin.py`; локальная ссылка с якорем `a.md#x` — маршрут описан |

## 2. Противоречия между частями / внутри части — устранены

| # | Где | Противоречие | Как приведено к одному |
|---|---|---|---|
| 8 | Ч1 D2 vs D3.1/D4/D15.1, Ч2 C2 | D2: Singleton `EventBus` + `EventBusProtocol` + `reset()`; всё остальное: две очереди, создаёт оркестратор, DI | D2 переписан: **Singleton снят**, очереди создаёт `ScanOrchestrator` и передаёт через DI (правило 09 п.6); имени `EventBus` в коде нет |
| 9 | Ч1 D3.3 | «Не работает только листинг организации» (текст до установки `gh`) рядом с «✅ `gh repo list` работает»; «три пути» при четырёх пунктах | Устаревшая фраза убрана; «четыре пути» + ссылка на `source.discovery` |
| 10 | Ч1 D15 vs Ч2 §8 | Бюджет «~1250–1400» против «≈1840» | Одна цифра (≈1710 сканер + 160 tokenstat ≈ 1870 после правок), в обеих частях |
| 11 | Ч1 D15.1 vs Ч2 C3/C4/папки | Наблюдатели: `CollectingObserver`/`StatisticsObserver`/`LoggingObserver` (Ч1) против только `CollectingObserver` (C3, папка) и `StatisticsObserver` (C4) | v1: **один** потребитель `CollectingObserver` (поток `collector`); остальные — примеры расширения; убрано из C4 |
| 12 | Ч1 D11 | «отключается через `ScanConfig.check_http`» — такого поля нет | `http.enabled: false` |
| 13 | Ч1 D11 vs Ч2 C3 | `AnchorChecker` только в части 2 | Добавлен в список чекеров D11; `checks.anchors` описан |
| 14 | Ч1 D5 vs D7.3 | D5: корень через `subprocess git rev-parse`; D7.3: GitPython | D5 → `GitAdapter` (GitPython) |
| 15 | Ч1 D5, Ч2 V6 | В D5 стоял ❓ по «папка вне git», в Ч2 уже решено (warning, код 0) | ❓ снят, решение зафиксировано в D5 |
| 16 | Ч1 D19.3 vs закон CLI | Пример `mdscan . out/hw01/logs out/hw01/reports …` — второй позиционный, по закону это код 2 | Пример переписан на `-logging.dir:… -report.dir:…` |
| 17 | Ч2 §2 vs D12.3 | Комментарий `source.target: используется ТОЛЬКО когда аргументов нет (тогда печатается конфиг)` — но `run_hw.py hw01` берёт цель именно оттуда | Комментарий переписан: цель для `run_hw.py`; `python -m core.mdscan` без аргументов — только печать |
| 18 | Ч1 D18.1/D18.3, Ч2 §9.4 vs D18.5 | `cost_calculator.py`, колонка `$`, метрика `cost_usd` — при решении «денег не считаем» | Убраны; в дерево добавлены `token_meter.py`, `token_totals.py` (как в Ч2) |
| 19 | Ч1 D8.3 vs Ч2 §8 | 7 правил в цепочке против «8 правил» в бюджете | Теперь 9 в обоих местах |
| 20 | Ч2 §8 | «4 модели» при 5 файлах в `models/` | 5 |
| 21 | Ч2 §9.2 vs `core/metrics` | `extract_precision`, `classify_accuracy` — таких функций нет | Считаем существующими `precision/recall/f1_score/accuracy/confusion_matrix`, новых не плодим |
| 22 | Ч2 §5 vs C3/папки | `SourceFactory` в таблице паттернов, но нигде больше | Добавлен в C3 и `source/source_factory.py` |
| 23 | Ч2 папки vs D19.3 | `usage_printer.py` и `config_printer.py` печатают одно и то же | Оставлен только `ConfigPrinter` (usage + конфиг) |
| 24 | Ч2 C4 | `MdLink.checked/ok` при наличии enum `CheckStatus` (OK/BROKEN/TIMEOUT/SKIPPED); `RepoInfo.url` и `remote_url` без различия; `LinkChecker.check → MdLink` при мутации на месте | `status: CheckStatus`; `remote_url` + `web_url`; `check(link, base_dir) -> None` заполняет поля |
| 25 | Ч1 D2.1 / D4 vs три пула | Имена потоков `md-worker_2`, `md-observer` от старой одностадийной схемы | `discover-N`, `parse-N`, `http-N`, `collector`, `progress` |
| 26 | Ч1 D1 | «`ThreadPoolExecutor(max_workers=5)`» — один пул | По пулу на стадию, размеры из конфига (D3.2) |
| 27 | Ч1 D16 M4 | «GitHub-организация — **если решаем делать**, без токена нереализуема» — устарело | ✅ делаем; риск сместился в пагинацию/rate-limit |
| 28 | Ч2 §7 план | В плане **не было** ни `GitHubOrgSource` (хотя ✅ берём, D3.3), ни фазы макетов M1–M5 (D16), ни extra `hw01` в `pyproject.toml` (D7.3 ссылается на `pip install -e .[hw01]`, а его нет) | Добавлены S0 (extra), **SP** (макеты, гейт до разморозки ТЗ), S12b (`GitHubOrgSource`) |

## 3. Стиль, устаревшие пометки, мусор — почищено

| # | Где | Что |
|---|---|---|
| 29 | Ч1 шапка | Статус «разделы 3–11 Alex ещё не читал» — разделов 3–11 в файле уже нет (в Ч2); таблица частей «⏸ на потом» — устарела; заголовки частей приведены к «часть 1/2 / часть 2/2», ТЗ под таски — отдельной строкой ⛔ |
| 30 | Ч1 §1 | «не требуется - но сломантся» → внятная формулировка |
| 31 | Ч1 D3.2 | Сырой неотформатированный текст Alex про yaml посреди раздела → цитата `> Alex:` + ссылка «принято → D19/D12» |
| 32 | Ч1 D3.4 | Строка «✅ Alex согласен — репозитории наши, доступ есть» стояла в разделе про pivot/pandas → убрана |
| 33 | Ч1 D3.5 | Хвост «- Тест: сентинелы не путаются…» после прогресса → перенесён в D3; `repos 3/11` → `3/10` |
| 34 | Ч1 D6 | Два одинаковых «✅ РЕШЕНО (Alex)» → один |
| 35 | Ч1 D9 | Пример шапки лога без секунд (`05-00.md`) при формате с секундами → `05-00-12` |
| 36 | Ч1 D10, D13 | Стёртые пометки `❓ Q4`, `❓ Q3` при уже стоящем ✅ Alex |
| 37 | Ч1 D12.3 | Абсолютные пути `E:\DSP-GPU` в примерах — запрещены правилом 05 (репо ездит на Debian) → `..\DSP-GPU` |
| 38 | Ч1 D4/D13/D14/D15 | Ссылки «раздел 3/5/6/8» указывали на разделы **старого** единого файла → «часть 2, §N» |
| 39 | Ч1 D7.1 | «разрешаются сам» → «сами» |
| 40 | Ч1 D2.1 | Обещание «продублирую в `.claude/rules/`» — честно помечено ⏳ **не сделано** (сделаю после приёмки, чтобы правило совпало с согласованным текстом) |
| 41 | Ч2 §2 | Добавлено поле `source.keep_clones: false` (в плане S12 и рисках очистка клонов упоминалась, поля не было) |
| 42 | Ч2 §10 риски | Добавлены: `ls-files` без untracked; нет `git` в PATH; 429/403 от GitHub на массовый HEAD; гонка `task_done()`/сентинелы |
| 43 | Ч2 C4 анализ, легенда Ч1 | Пометка `🔧 ревью 2` введена в легенду, все правки ею помечены |

---

## ✅ Вопросы к Alex — закрыты 2026-08-16 (ответы внесены в части 1 и 2)

1. **Бюджет кода** — ≈ 1870 строк против исходных ~1000 (**+87 %**). Правило 09: > 20 % → сначала
   искать лишнее, потом просить бюджет. Кандидаты на урезание (Ч1 D15): `RemoteRepoSource`/`GitHubOrgSource`
   (−190), `tokenstat` (−160), `ProgressReporter`+`Notifier` (−90). Пока считаю бюджет **не утверждённым**.
- from Alex - "исходных ~1000" - это про размер кода в одном файле
2. **`-h` / `--help`** — Ч2 §1.2 трактует как «без аргументов» (код 0). Формально нарушает п.3 закона CLI
   («первый не путь → код 2»). Оставить исключение или убрать?
- from Alex -  оставь как исключение
3. **Правило `LinkRule.matches(link: MdLink)`** вместо `matches(target: str)` — следствие ✅ решения про
   `WIKILINK`/`FOOTNOTE_URL`. Альтернатива — не выделять их в `LinkKind`, а хранить только `origin`
   и группировать в отчёте по нему. Я выбрала первое (сохраняет твоё решение из D7.1), скажи, если не так.
- from Alex - Да

## Что НЕ трогала

- Ни одно `✅ Alex`-решение по существу не изменено; правки — согласование текста с этими решениями,
  устранение противоречий и закрытие технических пробелов.
- Код не писала, ТЗ (`hw01_mdscan_spec_2026-08-16.md`) не размораживала, `.claude/rules/` не правила.
