# H-01 — Боевой прогон по реальной GitHub-организации `dsp-gpu`

> **Статус**: 🟡 ЧЕРНОВИК — не читан Alex
> **Дата**: 2026-08-17 · **Таск**: [`TASK_hw01_hardening_H01-H12.md`](../tasks/TASK_hw01_hardening_H01-H12.md) §A/H-01
> **Спека этапа**: [`hw01_mdscan_hardening_spec_2026-08-17.md`](hw01_mdscan_hardening_spec_2026-08-17.md) §1.2 п.2
>
> Код `core/mdscan/` **не менялся** — таск исследовательский. Всё найденное вынесено в §7 «Дефекты».

---

## 1. Что прогнали и чем

| # | Прогон | Команда (из корня репо) | Каталог |
|---|---|---|---|
| A | Организация, `DEBUG`-лог | `python -m core.mdscan https://github.com/dsp-gpu -source.auth:ssh -logging.level:DEBUG -logging.dir:out/hw01/h01 -report.dir:out/hw01/h01` | `out/hw01/h01/` |
| B | Один удалённый репозиторий | `python -m core.mdscan git@github.com:dsp-gpu/radar.git -logging.dir:out/hw01/h01b -report.dir:out/hw01/h01b` | `out/hw01/h01b/` |
| — | Эталон для сверки (оркестрант, 04:18) | та же цель, лог `INFO`, без `-source.auth` | `out/hw01/dsp-gpu/` |

Среда: Windows 11, Python 3.14 (`.venv`), Git Bash, `export PATH="$PATH:$HOME/.local/gh/bin"`.
Предпроверка доступа: `gh repo list dsp-gpu --limit 100 --json name,sshUrl` → **10 приватных репозиториев**, код 0.

Артефакты прогона A: `out/hw01/h01/dsp-gpu_2026-08-17_08-07-05.md` (2.03 МБ) и `..._08-07-05.log` (7.16 МБ, 16 993 строки).
Артефакты прогона B: `out/hw01/h01b/radar_2026-08-17_08-11-35.md` / `.log`.

---

## 2. Прогон A — числа

### 2.1 Репозитории, клоны, файлы

| показатель | значение |
|---|---|
| репозиториев в организации (по `gh`) | 10 |
| склонировано (`git clone --depth 1`, SSH) | 10 |
| вложенных репозиториев (`repos_nested`) | 0 |
| найдено `.md` (`md_files_total` / `files_ok`) | 1230 / 1230 |
| файлов с ошибкой чтения (`files_failed`) | 0 |
| `error_rate` | 0 |

Разложение файлов по репозиториям (из секции «Файлы» отчёта):

| репозиторий | `.md` |
|---|---|
| `workspace` | 846 |
| `DSP` | 64 |
| `radar` | 63 |
| `spectrum` | 57 |
| `core` | 55 |
| `strategies` | 39 |
| `signal_generators` | 37 |
| `linalg` | 31 |
| `stats` | 20 |
| `heterodyne` | 18 |
| **итого** | **1230** |

### 2.2 Ссылки по типам

| категория | всего | ok | broken | timeout | skipped |
|---|---|---|---|---|---|
| local | 1563 | 1012 | 551 | 0 | 0 |
| anchor | 403 | 344 | 59 | 0 | 0 |
| github | 55 | 55 | 0 | 0 | 0 |
| url | 162 | 138 | 23 | 1 | 0 |
| wikilink | 448 | 0 | 0 | 0 | 448 |
| footnote_url | 38 | 0 | 0 | 0 | 38 |
| **итого** | **2669** | **1549** | **633** | **1** | **486** |

`broken_total` в счётчиках = **634** = 633 битых + 1 `TIMEOUT` (см. дефект **D-4**).
`broken_ratio` = 0.238.

### 2.3 Разложение битых HTTP по кодам

217 сетевых проверок (162 `url` + 55 `github`), исходы:

| исход | кол-во | где в отчёте |
|---|---|---|
| `200 OK` | 192 | — |
| `202` | 1 | — |
| `403` | 21 | секция «HTTP 401/403/429» |
| `401` | 1 | секция «HTTP 401/403/429» |
| без кода — `SSL: CERTIFICATE_VERIFY_FAILED` | 1 | секция «Битые HTTP-ссылки» |
| `TIMEOUT` (2000 мс) | 1 | секция «Таймауты» |

Секция **«HTTP 401/403/429 — вероятно защита от ботов или лимит (проверить вручную)»** заполнилась
корректно, 22 строки, домены — ровно те, что ожидались (научные издательства и порталы с бот-защитой):

| домен | строк | код |
|---|---|---|
| `image-ppubs.uspto.gov` | 3 | 403 |
| `www.researchgate.net` | 2 | 403 |
| `www.ejournal.org.cn` | 2 | 403 |
| `onlinelibrary.wiley.com` | 2 | 403 |
| `www.tandfonline.com` | 2 | 403 |
| `www.mdpi.com` | 2 | 403 |
| `www.sciencedirect.com` | 2 | 403 |
| `www.mathworks.com` | 2 | 403 |
| `doi.org` | 1 | 403 |
| `developers.redhat.com` | 1 | 403 |
| `gitlab.kitware.com` | 1 | 403 |
| `dl.acm.org` | 1 | 403 |
| `huggingface.co` (`/settings/tokens`) | 1 | **401** |

В секции «Битые HTTP-ссылки» осталась **одна** строка — `tomroelandts.com`, ошибка проверки сертификата
(не HTTP-код). `429` в этот раз не встретился ни разу.
`TIMEOUT`: `www.analog.com/.../phased-array-antenna-patterns-part3.html`, отдельная секция «Таймауты».

### 2.4 Время

Реальное время (`date` вокруг команды) — **103 с**; `duration_sec` в отчёте — **95.79 с**.
Разбор фаз по временным меткам лога (относительно старта):

| фаза | интервал, с | длительность | доля |
|---|---|---|---|
| `gh repo list` | 0.00 → 0.62 | 0.6 с | 0.6 % |
| **клонирование 10 репозиториев (последовательно)** | 0.62 → 73.14 | **72.5 с** | **70 %** |
| обход + постановка файлов в очередь | 73.14 → 77.92 | 4.8 с | 5 % |
| разбор + проверка ссылок (10 потоков) | 77.92 → 95.79 | 17.9 с | 17 % |
| сборка и запись отчёта | 95.79 → 95.81 | 0.02 с | <1 % |
| **вывод сводки в консоль (rich)** | 95.81 → 102.78 | **7.0 с** | 7 % |
| удаление 10 клонов | 102.90 → 103.15 | 0.25 с | <1 % |

`throughput_files_per_sec` = 12.84 (считается от `duration_sec`, т.е. включает клонирование).

### 2.5 Ошибки в логе

| уровень | строк |
|---|---|
| `CRITICAL` | **0** |
| `ERROR` | **0** |
| `WARNING` | 1269 |
| `INFO` | 1293 |
| `DEBUG` | 14 425 |

Разбор `WARNING` (все — штатные, «битая ссылка», не сбои):

| источник | строк |
|---|---|
| `LocalFileChecker`: «битая локальная ссылка ...» | 536 |
| `AnchorChecker`: «битый якорь ...» | 74 |
| `HttpChecker`: «http broken ...» | 23 |
| `HttpChecker`: «нет ответа за 2000 мс ...» + «http timeout ...» | 2 |
| `MarkdownWorker`: «link broken/timeout kind=... » (сводная строка воркера) | 634 |

Числа сходятся: воркер логирует **каждую** битую ссылку (551 local + 59 anchor + 23 url + 1 timeout),
чекеры — каждую **проверку**; 536 + 15 = 551 (15 локальных ссылок битые из-за якоря, их «битый якорь»
логирует `AnchorChecker`: 59 чистых anchor + 15 = 74). Дублирование по два `WARNING` на одну битую
ссылку — см. дефект **D-3**.

`DEBUG`-лог наполнен по всем стадиям, как требует правило 11: `parse-start` ×1230, «файл найден» ×1230,
«прочитан ...» ×1230, «извлечено ссылок» ×1230, по правилу классификации на каждую ссылку, `http NNN` на
каждую сетевую проверку, `link ok/broken ...` на каждую ссылку. Итоговая строка `INFO` присутствует:
`итоги: файлов 1230, ссылок 2669, битых 634, за 95.79 с, код 1`.

---

## 3. Прогон B — один удалённый репозиторий (`git@github.com:dsp-gpu/radar.git`)

| показатель | значение |
|---|---|
| вид цели | `remote_repo` |
| клон | `out/hw01/_clones/dsp-gpu__radar`, `depth=1`, создан 08:11:35, удалён 08:11:42 |
| `.md` файлов | 63 |
| ссылок всего | 181 (local 90 / anchor 60 / github 4 / url 27) |
| битых | 36 local + 5 anchor + 12 url = 53, плюс 2 `TIMEOUT` → `broken_total` 55 |
| секция «HTTP 401/403/429» | 12 строк (researchgate, wiley, tandfonline, doi.org, mdpi ×2, sciencedirect ×2, mathworks) |
| секция «Битые HTTP-ссылки» | пусто (`_нет_`) |
| «Таймауты» | 2 (`www.ejournal.org.cn`) |
| время | 7.22 с (из них клон 3.2 с) |
| код возврата | **1** |
| `ERROR` / `CRITICAL` в логе | 0 / 0 |

**Сверка «репозиторий в одиночку» vs «тот же репозиторий внутри организации»**: `radar` в прогоне A —
63 файла, 181 ссылка, 55 битых; в прогоне B — те же **63 / 181 / 55**. Ветка `remote_repo` даёт тот же
результат, что и ветка `github_org` для одного репозитория. ✅

Единственное расхождение — исход двух ссылок на `www.ejournal.org.cn`: в прогоне A `403`, в прогоне B
`TIMEOUT`. Это сетевая флуктуация домена (таймаут 2000 мс), не дефект; но показывает, что HTTP-исходы
по живой сети **не детерминированы** — для регрессии нужен локальный сервер (H-03/H-07).

---

## 4. Сверка с прогоном оркестранта (`out/hw01/dsp-gpu/..._04-18-33.md`)

| показатель | эталон 04:18 | прогон A 08:07 | совпало |
|---|---|---|---|
| репозиториев / клонов | 10 / 10 | 10 / 10 | ✅ |
| `.md` файлов | 1230 | 1230 | ✅ |
| ссылок всего | 2669 | 2669 | ✅ |
| local всего / битых | 1563 / 551 | 1563 / 551 | ✅ |
| anchor всего / битых | 403 / 59 | 403 / 59 | ✅ |
| github всего / битых | 55 / 0 | 55 / 0 | ✅ |
| url всего / битых | 162 / 23 | 162 / 23 | ✅ |
| `TIMEOUT` | 1 | 1 | ✅ |
| wikilink skipped | 448 | 448 | ✅ |
| footnote_url skipped | 38 | 38 | ✅ |
| `broken_total` / `broken_ratio` | 634 / 0.238 | 634 / 0.238 | ✅ |
| `ERROR` / `CRITICAL` | 0 / 0 | 0 / 0 | ✅ |
| `WARNING` | 1269 | 1269 | ✅ |
| код возврата | 1 | 1 | ✅ |
| клоны удалены | да | да | ✅ |
| строк отчёта | 1959 | **1964** | ⚠️ +5 |
| `duration_sec` | 66.69 | **95.79** | ⚠️ +29.1 |

**Расхождение 1 — строк отчёта 1959 → 1964 (+5).** Объяснение: между прогонами внесена правка ревью 6 —
новая секция отчёта «HTTP 401/403/429 ...» (заголовок + шапка таблицы + разделитель + пустые строки).
Содержательные числа не изменились: 22 строки переехали из «Битые HTTP-ссылки» в новую секцию,
в старой осталась 1 строка (SSL). Это **ожидаемое** расхождение, а не находка.

**Расхождение 2 — 66.69 с → 95.79 с (+44 %).** Разница целиком в фазе клонирования: эталон 0.58 → 44.40 с
(43.8 с), прогон A 0.62 → 73.14 с (72.5 с); только `workspace` клонировался 9.9 с против 19.6 с.
Фаза разбора и проверки практически одинакова: 22.3 с против 22.65 с. Вывод: **разница — пропускная
способность сети в момент клонирования**, не регрессия модуля. Наблюдение, из которого растёт дефект **D-2**
(клонирование не распараллелено).

**Расхождение 3 — состав лога.** У эталона `DEBUG` нет вовсе (`INFO` 1291 / `WARNING` 1269), у прогона A
`DEBUG` 14 425 при `-logging.level:DEBUG`. Также в прогоне A появились строки `прогресс выключен: поток
вывода не терминал` (stdout перенаправлен в файл) и `вывод в консоль: rich` / `консоль (rich): ...`
(добавлены ревью 6). Всё ожидаемо.

---

## 5. Три ручные проверки (шаг 5 таска)

### 5.1 Клоны легли в `out/hw01/_clones` и удалились

- В логе прогона A: `клонирую ...` ×10 → `клон удалён: ...` ×10 (по одному на каждый репозиторий).
- В логе прогона B: `клонирую ...` ×1 → `клон удалён: ...` ×1.
- После обоих прогонов `ls -la out/hw01/_clones/` → каталог **пуст**. ✅
- В `DEBUG` видно, как удаление обходит Windows-специфику: `снимаю read-only и повторяю удаление:
  ....git/objects/pack/*.idx|.pack|.rev ([WinError 5] Отказано в доступе)` — механизм отрабатывает,
  ошибок наружу не выходит. Хорошо, что это `DEBUG`, а не `WARNING`.

### 5.2 В логе и отчёте нет токенов и паролей

```bash
grep -ic "ghp_\|gho_\|ghs_\|github_pat_\|Authorization\|password\|passwd\|secret\|api[_-]key\|Bearer " <файл>
grep -io ".\{0,45\}token.\{0,25\}" <файл> | sort -u
```

| файл | совпадений по «секретным» шаблонам |
|---|---|
| `out/hw01/h01/..._08-07-05.log` | 0 |
| `out/hw01/h01/..._08-07-05.md` | 0 |
| `out/hw01/h01b/radar_..._08-11-35.log` | 0 |
| `out/hw01/h01b/radar_..._08-11-35.md` | 0 |

Слово `token` встречается только в **именах сканируемых файлов** (`TASK_tfft_rewrite_G4_features_tokens.md`,
`HANDOFF_2026-08-13_lfm_cubes_phase2_token.md`) и в **URL проверяемой ссылки**
`https://huggingface.co/settings/tokens` (это адрес страницы, не значение токена). SSH-ключи, `Authorization`,
пароли, `ghp_*` — отсутствуют; `gh` вызывается без передачи токена в командную строку. ✅

Единственная «подозрительная» находка — длинная base64-строка в отчёте: это `data:image/png;base64,...`
из сканируемого документа, а не секрет (см. дефект **D-1**).

### 5.3 Код возврата

```bash
python -m core.mdscan https://github.com/dsp-gpu ... ; echo $?          # → 1
python -m core.mdscan git@github.com:dsp-gpu/radar.git ... ; echo $?    # → 1
```

Оба прогона: **1** (есть битые ссылки). Совпадает с полем «код возврата» в отчёте и со строкой
`итоги: ... код 1` в логе. ✅

### 5.4 Побочный эффект — `mdscan.yaml` в корне

После каждого прогона в корне репозитория появляется `mdscan.yaml` (11 330 байт, дамп эффективного
конфига). Оба раза удалён (`rm -f mdscan.yaml`); в `git status` изменений по `core/` нет. Это ожидаемое
поведение по §0.4 таска, но само по себе неприятно — см. **D-7**.

---

## 6. Что подтверждено

1. Ветка `github_org` работает на живой приватной организации: `gh` → 10 репозиториев → SSH-клоны
   `--depth 1` → обход → разбор → проверка → отчёт → удаление клонов. Ни одного `ERROR`/`CRITICAL`.
2. Числа **полностью воспроизводимы** по всем счётчикам ссылок и файлов (§4) — модуль детерминирован
   на локальной части; недетерминирована только живая сеть.
3. Ветка `remote_repo` даёт тот же результат, что и `github_org` на том же репозитории (§3).
4. Решение Alex по `401/403/429` реализовано и работает: 22 строки в отдельной секции, в статистике
   они остались в `broken` (§2.3).
5. Фикс fuzzy-`linkify` (этап 1) держится: голых `Full.md` / `conftest.py` / `prng.cl` среди `url` нет,
   `links_url` = 162, `links_unknown` = 0.
6. Логирование по правилу 11 присутствует на всех стадиях, включая итоговую строку с теми же числами,
   что в отчёте.

---

## 7. Дефекты и замечания (ничего не чинил)

| # | Серьёзность | Что | Где | Предложение |
|---|---|---|---|---|
| **D-1** | 🔴 высокая | `data:image/png;base64,...` классифицируется `LocalPathRule` как `local`, проверяется как путь к файлу → **ложный BROKEN**, и полный base64 (1 811 479 символов) уходит в отчёт одной строкой. 89 % объёма отчёта (1.81 МБ из 2.03 МБ) — это одна ссылка из `heterodyne/Doc/Алгоритм гетероди.md:150` | `core/mdscan/parsing/rules/rule_local_path.py` (тотальное правило-«дно»), правила для схемы `data:` нет | добавить правило схем (`data:`, `javascript:`, `file:` ...) → `skipped`/`unknown` перед `LocalPathRule`; независимо — обрезать `target` в отчёте до N символов |
| **D-2** | 🟠 средняя | Клонирование 10 репозиториев **последовательно** в одном потоке `discover_0`: 72.5 с из 103 с (70 % прогона). `workers.discover=5` не даёт ничего, потому что параллелизм стадии 1 — **по источникам** (`pool.submit(self._scan_source, source)`), а организация — один источник | `core/mdscan/runtime/pipeline_runner.py:123-133` (`_discover` / `_scan_source`) | распараллелить по репозиториям внутри источника (либо `GitHubOrgSource` клонирует пулом). Ожидаемый выигрыш на этой цели — десятки секунд. **Трогает архитектуру стадии 1 → решает Alex** |
| **D-3** | 🟡 низкая | Каждая битая ссылка логируется **дважды** `WARNING`: чекером («битая локальная ссылка ...») и воркером («link broken kind=... »). 1269 строк `WARNING` на 634 битых ссылки | `core/mdscan/checking/local_file_checker.py`, `anchor_checker.py`, `http_checker.py` vs `core/mdscan/runtime/markdown_worker.py` | оставить `WARNING` в одном месте (воркер — у него есть контекст repo/file), у чекеров понизить до `DEBUG`. Пересекается с H-06 |
| **D-4** | 🟡 низкая | `broken_total` = 634 при 633 фактически битых: в него включён 1 `TIMEOUT`. В таблице по категориям `broken` и `timeout` — разные колонки, в счётчиках слиты. Консоль печатает «битых ссылок 634» | сборщик счётчиков `ScanSummary`; уже помечено в таске H-11 | либо переименовать в `broken_or_timeout`, либо не включать `TIMEOUT`. Решение за Alex — от него зависят числа в README ДЗ |
| **D-5** | 🟡 низкая | `duration_sec` (95.79 с) не покрывает вывод сводки в консоль (**7.0 с**, `rich` рисует 634 строки битых ссылок) и удаление клонов. Реальное время — 103 с, расхождение 7 % | `core/mdscan/runtime/scan_orchestrator.py` (момент фиксации длительности) | либо считать длительность до конца работы, либо отдельной строкой «вывод сводки, с». Отдельно: печатать в консоль **все** 634 битые ссылки сомнительно — ограничить первыми N + «ещё N в отчёте» |
| **D-6** | 🟡 низкая | По `DEBUG`-логу **нельзя отличить сетевой запрос от попадания в кэш**: строка `http NNN <url>` печатается уже после обращения к кэшу — на 217 проверок при 132 уникальных URL получилось 217 одинаковых строк | `core/mdscan/checking/http_checker.py:54-69` (`check` → `_cached` → общий `_log.debug`) | добавить `DEBUG`-строку в `_request` («сетевой запрос ...») либо пометку `(кэш)`. Нужно H-03, где кэш проверяется по логу |
| **D-7** | ⚪ замечание | Прогон пишет `mdscan.yaml` (11 КБ) **в текущий каталог**, т.е. в корень репозитория, независимо от `-report.dir` | слой конфигурации / CLI | писать дамп конфига в `report.dir` или только по явному флагу; иначе каждый прогон мусорит в рабочем дереве |
| **D-8** | ⚪ замечание | Имя потока обхода — `discover_0` (подчёркивание), у остальных стадий `parse-1`, `collector` (дефис). Правило 11 требует `discover-1` | `core/mdscan/runtime/pipeline_runner.py:53`, `DISCOVER_PREFIX = "discover"` + `ThreadPoolExecutor` добавляет `_N` | косметика: либо задать префикс `discover-`, либо поправить формулировку правила |
| **D-9** | ⚪ замечание | В спеке этапа §1.2 п.2 единственная не-`403` битая HTTP-ссылка названа «1 DNS». Фактически это **ошибка проверки TLS-сертификата** `tomroelandts.com` (`SSL: CERTIFICATE_VERIFY_FAILED`), а не DNS | `hw01_mdscan_hardening_spec_2026-08-17.md` §1.2 | поправить формулировку в спеке при следующем ревью |

**Ничего не упало**: обе цели отработали до конца, кода возврата 2/3 не возникло, зависаний не было
(лимит 15 минут не понадобился: 103 с и 7 с).

---

## 8. Готовность таска

- [x] Доступ к организации проверен (`gh repo list dsp-gpu` → 10).
- [x] Прогон по организации с `-logging.level:DEBUG` завершён, код 1.
- [x] Прогон 2б по одному удалённому репозиторию завершён, ветка `remote_repo` проверена, клон удалён.
- [x] Числа записаны и сверены с прогоном оркестранта: все счётчики ссылок и файлов совпали до единицы,
      два расхождения объяснены (новая секция отчёта; скорость сети при клонировании).
- [x] Разложение битых HTTP по кодам сделано; секция «HTTP 401/403/429» проверена по доменам.
- [x] Три ручные проверки выполнены (клоны удалены · секретов нет · код возврата 1).
- [x] Список дефектов составлен (9 пунктов, ни один не исправлялся).
- [x] Регрессия: `python -m pytest tests/hw01 -q` → **398 passed, 4 skipped** (код не менялся).

---

*Черновик 2026-08-17, H-01. Артефакты: `out/hw01/h01/`, `out/hw01/h01b/`; эталон — `out/hw01/dsp-gpu/`.*
