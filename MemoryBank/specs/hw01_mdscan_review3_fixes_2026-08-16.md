# Ревью 3 — диаграммы части 2 (C1–C4, 3.5 D1–D5) ↔ решения части 1

> **Дата**: 2026-08-16 · **Автор**: Кодо · **Задача Alex**: «глубокое ревью спеки 2 (архитектура),
> пристальное внимание на связи в диаграммах и реально заложенные под них решения в спеке 1; 3.5 тоже».
> **Статус**: ✅ прочитан Alex 2026-08-16, три следствия приняты (ответы внизу); доработка «законченный
> цикл» внесена (C2, D1, новая D6, D4 части 1). Правки в обеих частях помечены `🔧 ревью 3`.
> Ни одно ✅-решение Alex по существу не изменено; менялись связи, сигнатуры и формулировки, которые
> расходились между диаграммой и текстом или между частями.
>
> Файлы: часть 1 `hw01_mdscan_reasoning_2026-08-16.md` · часть 2 `hw01_mdscan_architecture_2026-08-16.md`.

## Метод

Каждую стрелку C1–C4 и каждый шаг D1–D5 (3.5) проверила по трём вопросам:
(а) есть ли под ней решение в части 1 и совпадает ли с ним; (б) не противоречит ли она другой
диаграмме части 2; (в) есть ли этот компонент в §4 (папки) / §5 (паттерны) / §7 (план) / §8 (бюджет).

---

## 1. Расхождения диаграмм с решениями части 1 — исправлены

| # | Диаграмма | Что было | Решение части 1 | Что сделано |
|---|---|---|---|---|
| 1 | C2, C3, C4 | `StatisticsCollector → md-отчёт / консоль`: отчёт писал поток Reporter | **D4**: «только теперь [после `reporter.join()`] печатаем финальную таблицу и пишем отчёт» — главный поток | Отчёт/консоль строит `ScanOrchestrator` (главный поток) после `join()` из `collector.results` + `statistics.summary()`. Все три диаграммы, D1 (sequence), инвариант 25 |
| 2 | C2 (шаг 4), C4 | `LinkClassifier.classify(target)` — строка | **D8** (✅ Alex): `matches(link)`, правила смотрят `link.origin` | `link.kind = classify(link)` везде |
| 3 | C2, C4, D3.1 части 1 | Часть 1 D3.1 п.3: «отдаёт в **пул** http, ждёт `future.result()`»; часть 2: семафор, вариант A | **C2 вариант A** (решение Alex): семафор внутри parse-worker'а | Часть 1 D1/D3.1/D4 приведены к семафору; имя потока `http-3` убрано из D4 (таких потоков нет) |
| 4 | C3 (`config`) | `CHAIN → OVR → SC`: валидация **до** применения `-поле:значение` | **D4 фаза 0** п.3–5 + V9/V10 проверяют `logging.dir`/`report.dir`, которые могут прийти из CLI | Черновик конфига → цепочка V1…V10, внутри **V3** вызывается `CliOverrideApplier`, потом freeze |
| 5 | C3 (`source`) | `SourceFactory: выбор по source.kind / auto` — вторая детекция цели, помимо V5 `TargetKindRule` | правило 07 (один источник истины), D12 (четыре ветки) | V5 записывает `SourceKind` в конфиг; фабрика только читает. Инвариант 23 |
| 6 | C3 (`source`) | один источник на прогон | **§2.0 / D12**: при цели `yaml` — `target` + `repositories[]` смешанным списком, «сканируем всё вместе» | `SourceFactory.for_config → list[RepositorySource]`, по источнику на цель; оркестратор сливает `RepoInfo` |
| 7 | C3 (`discovery`) | `NestedRepoFinder`/`MarkdownFileFinder` не связаны с `GitAdapter` | **D5, D7.4**: submodules и `ls-files` через GitPython | Добавлены связи; в §4 отмечено, что `git_adapter` используют оба пакета |
| 8 | C1 | «отчёт по токенам» шёл из сканера | **D18** + C3: `tokenstat` — отдельный инструмент, читает транскрипт | Узел `core.tokenstat` + транскрипт; из сканера — только лог и md-отчёт |
| 9 | C1 | нет связи «сканер → GitHub API / `gh`» | **D3.3**: листинг организации — не git-протокол, а `gh`/REST | Добавлена; таблица связей: 6, не 5 |
| 10 | C2 (`Progress`) | читает только `StatisticsCollector` | **D3.5**: строка статуса показывает `queue task=… result=…`, `repos n/N` | + `qsize()` очередей, счётчик репозиториев |
| 11 | C2, C3, C4 | зона 2 (`Notifier`) нигде не связана с модулями | **D3.5**: «любой модуль пишет через `notifier.show()`», Null Object | `MarkdownWorker`, `HttpChecker → Notifier`; `ProgressReporter` реализует, `NullNotifier` при выключенном |
| 12 | C4 | `ScanOrchestrator.run(config)` | **D18.5**: публичный контракт `Scanner.scan(config) → ScanSummary` | `Scanner (Protocol)` в `scanner.py`, `ScanOrchestrator.scan()` реализует; `task.py`/`__main__` зовут только протокол |
| 13 | C4, C2 | `MarkdownWorker` — отдельный класс с собственным циклом; parse — `ThreadPoolExecutor` | **D15.1**: цикл потребителя написан **один раз** в `BaseObserver`; **D3.1**: сентинел на каждый worker | `MarkdownWorker(BaseObserver)` × `workers.parse` именованных потоков; D4 части 1 обновлён (`parser_pool.start()` → `.start() × N`, `join(timeout)`) |
| 14 | C2 (`AnchorChecker`) | «заголовки этого же файла» | **D8.1** (ревью 2): `a.md#x` → якорь в **целевом** файле | Метка исправлена; §2 `checks.anchors` уточнён |
| 15 | D2 (state) | `Пропущена: mailto · tel · unknown` | **D8.1**: `WIKILINK`/`FOOTNOTE_URL` → `NullChecker`; §0.3: выключенные проверки | + wikilink/footnote_url; + ветка «проверка выключена → SKIPPED»; локальная с якорем |
| 16 | D1 (sequence) | нет старта parse-worker'ов и логгера; нет `task_done()` на сентинелах; нет `listener.stop()` | **D4** (фаза 0 п.6, фаза 1), инвариант 19, таблица гашения | Добавлены |
| 17 | D4 (activity) | выход по сентинелу **без** `task_done()`; ошибка только «чтения» | инвариант 19; **D2.1**: исключение на любом шаге → результат с `error` | `task_done` для сентинела; ветка «исключение» от C, D, H |

## 2. Пробелы проектирования, которые вскрылись при сверке — закрыты

| # | Где | Пробел | Что сделано |
|---|---|---|---|
| 18 | C4 `LinkChecker.check(link, base_dir)` | якорному чекеру для `#x` нужен **файл**, а не каталог; правило сравнения якоря нигде не записано | `check(link, md_file)`; `AnchorChecker`: кэш заголовков по файлу, сверка по **GitHub-slug**; записано в D8.1 части 1, инвариант 24, риск, S10 |
| 19 | C4 `HttpChecker` | не оговорено число экземпляров → у каждого worker'а свой семафор → параллелизм `workers.parse × http.workers`; кэш без `Lock` | один экземпляр на прогон через `CheckerFactory`; кэш под `Lock`; инвариант 22, риск, D11 части 1 |
| 20 | §2 `http` | дефолтный UA `Python-urllib/3.x` → 403 у многих сайтов и GitHub → ложные BROKEN | поле `http.user_agent: "mdscan/0.1"`, риск, D11 |
| 21 | C4 `ConsoleRenderer.render(summary)` | по T9 в консоли нужен **список** битых ссылок с кодами | `render(results, summary)` |
| 22 | C4 `CollectingObserver` | без поля — неоткуда брать `results` для `build(results, summary)` | `+results: list[MdFileResult]` |
| 23 | C4 | из 9 правил `LinkRule` нарисовано 5; нет `SourceFactory`, `RendererFactory`, `Notifier`, `ProgressReporter`, `Scanner` | добавлены |
| 24 | C3 | нет пакета `log_setup` (LoggingSetup, LogNaming) и `RendererFactory`, хотя они в §4/§5 | добавлены с связями от оркестратора |
| 25 | §2, S12b, D3.3 части 1 | `source.discovery` в части 1 ещё имел режим `list`, в части 2 — уже нет (список = отдельные цели при `yaml`) | Часть 1 D3.3 переписана: `auto | gh | api`, три пути; S12b — `gh → api` |
| 26 | §4 | `notifier.py` один файл на Protocol + Null; отрисовка прогресса rich/plain без своих классов (D10 «за одним интерфейсом») | `notifier.py · null_notifier.py`; `progress_view.py · rich_progress_view.py · plain_progress_view.py` (Strategy); §5, §8 (+30) |
| 27 | §10 | нет риска «оркестратор разрастается» | лимит ~120 строк (правило 09 п.3), при превышении фаза 1 → `PipelineRunner` |
| 28 | §0 | ссылки: A6 «D9, D14» (D14 — «один класс = один файл»), A12 «D9 части 1» (D9 — логирование), A2 без S12b, A3 без инвариантов 18–20 | исправлены |
| 29 | §1.1.1, §2.0 | `E:\DSP-GPU` в примерах — абсолютный Windows-путь в документации (правило 05) | `..\DSP-GPU` |
| 30 | D5 (gantt) | repo-3 стартует в 3 с при `workers.discover: 5` по умолчанию — не сходится | подпись «workers.discover=2» |
| 31 | §8 | бюджет | ≈ 1910 (+40 к утверждённым 1870, +2 %) — отмечено, отдельно не прошу |

## 3. Что проверила и **не** трогала (сходится)

- C2/D4: порядок `put(END_DISCOVERY) × workers.parse → TaskQueue.join() → END_RESULTS → ResultQueue.join() → reporter.join() → progress.stop()` — совпадает с D4 части 1.
- C4: `MdTask`, `RepoInfo`, `ScanConfig` — `frozen` (пересекают границу потока, правило 09 п.4); `MdLink`/`MdFileResult` — по правилу владения D15.2. Совпадает.
- C4: `ProcessedRegistry.add_if_absent(key)`, key = `(repo_root, md_file)` — D6.4. Совпадает.
- C3: `GitHubOrgSource → RemoteRepoSource` «по репозиторию» — D3.3 «N раз этап 2». Совпадает.
- D3 (state MdFileResult): жизненный цикл = D15.1/D15.2/D2.1. Совпадает.
- §2 `workers.*`, `http.workers` как семафор — D3.2 (уже согласовано в части 1). Совпадает.
- Закон CLI (4 ветки, `yaml`, `-h`) — D12 части 1 совпадает с §1.

## ✅ Следствия — прочитаны Alex 2026-08-16, ответы ниже

1. **`MarkdownWorker` теперь наследник `BaseObserver`** (п. 13) — это твоё же требование D15.1
   («базовый класс Observer, наследовать»), просто доведённое и до стадии 2. Стадия 2 — не
   `ThreadPoolExecutor`, а `workers.parse` именованных потоков. Если хочешь оставить executor — скажи,
   верну, но тогда цикл потребителя будет в двух местах.
- from Alex - я прсто предложил как один из вариантов в решении сократить объем кода используя принципы ООП. Сделай правильно и красиво я же пока не вижу код! доверяю твоему опыту. Обрати внимание что тебе нудно будет изменить все диаграмы!
2. **Отчёт строит главный поток** (п. 1) — как в твоём D4. Поток collector только копит и считает.
- from Alex - доработай что бы был законченный цикл 
3. **`check(link, md_file)`** вместо `base_dir` (п. 18) — следствие якорей; иначе `AnchorChecker`
   нереализуем без второго параметра.
- from Alex - Хорошо!
