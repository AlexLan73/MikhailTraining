"""Контракт зоны 2 вывода: «показать человеку короткую строку»."""

from __future__ import annotations

from typing import Protocol


class Notifier(Protocol):
    """Куда любой модуль отправляет сообщение о событии (D3.5).

    Владеет контрактом T-01, реализует `ProgressReporter` (T-11) и `NullNotifier`.
    Потребитель зависит только от этого протокола — прогресс можно выключить,
    не трогая ни один вызывающий модуль.
    """

    def show(self, text: str) -> None:
        """Показать строку в зоне сообщений; вызов обязан быть дешёвым и не бросать."""
        ...
