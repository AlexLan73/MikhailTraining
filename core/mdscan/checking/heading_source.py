"""Контракт «дай заголовки Markdown-текста» — нужен `AnchorChecker`.

Владелец контракта — T-07 (потребитель), реализация — `MarkdownItHeadingSource`
из T-06 (структурно, спека разработки §2.5). Так проверка ссылок не зависит от
парсера: в тестах вместо него подставляется простая заглушка.
"""

from __future__ import annotations

from typing import Protocol


class HeadingSource(Protocol):
    """Источник заголовков файла — единственное, что нужно якорному чекеру."""

    def headings(self, text: str) -> tuple[str, ...]:
        """Тексты заголовков всех уровней в порядке появления (без решёток)."""
        ...
